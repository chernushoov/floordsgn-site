# Competitor Monitor

Scaffold for the competitor‑monitoring bot.

- `crawler.js` – scrapes competitor sites.
- `parser.js` – extracts relevant data and writes markdown.
- `scheduler.js` – runs the crawler daily via node‑schedule.
- `cron.json` – definition for the OpenClaw cron job.
