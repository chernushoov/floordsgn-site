# FIXLOG — НАРЯД 07: RU нативный перевод-пасс (FIX 9)

- **Ветка:** `fix/ru-native-translation` (от `integration` @ 29546c5)
- **Worktree:** `floordsgn-site-new__runative`
- **Дата:** 2026-06-10 → 2026-06-11
- **Статус:** ✅ ЗАВЕРШЕНО по телу статей — все 108 RU-файлов переведены до натива, рескан = 0. Остался один структурный слой (nav-метки lib:foot/lib:crumb) — см. §6.
- **Не мёржено, не задеплоено.** Прод, configurator и эталоны не тронуты.

---

## 1. Задача (наряд)
Поднять RU с «машинного огрызка» до уровня носителя-инженера. RU — стратегический клин. 6 подзадач:
1. Огрызки → полный перевод тела.  2. Убить рунглиш.  3. 4 флагмана RU-only с мёртвым EN-тогглом.  4. RU «Back to Library» + стрэй-английские заголовки.  5. «Меда декоративи» → русский термин.  6. Структурный i18n где надо.

## 2. Решения (durable)
| Вопрос | Решение | Почему |
|---|---|---|
| Флагманы: EN-тело vs залочить RU | Залочить RU + скрыть мёртвый EN-тоггл (per-page инлайн-скрипт) | Additive-only, без фабрикации EN. Общий хедер-партиал не тронут. |
| «Back to Library» в RU-блоке | → «Назад в библиотеку» (только внутри `data-lang-block="ru"`; EN-блок остаётся EN) | |
| «Меда декоративи» | → «декоративный литой бетон» | Реальный термин вместо иврит-транслита. |
| Терминология | микроцемент / микротопинг / наливной пол / стяжка / затирка / грунт / заполнитель / пропитка-силер / сопротивление скольжению | Профессиональная, не калька. |
| Стандарты, бренды, SKU, химия, единицы | Оставлены как есть | Не транслитерируются. |

## 3. Метод
Worktree off `integration`. Детерминированные правки вручную (Меда, флагман-локи). Перевод/чистка — **Workflow-оркестрация**, pipeline на файл: `Translate (opus, нативный RU)` → `Verify (sonnet, adversarial рунглиш-аудит + фикс)`. Жёсткий общий бриф (роль носителя-инженера, хард-фейл рунглиш-паттерны, словарь, технические границы). Пилот на 4 файлах → полный прогон волнами (3 волны из-за session-лимитов токенов; каждая волна коммитилась, остаток пересчитывался и добивался).

## 4. Выполнено и проверено (108/108)
- **Меда** → «декоративный литой бетон»: 7×, 3 файла.
- **4 флагмана** залочены на RU (epoxy-sl, microtopping, pu-cement, terrazzo): инлайн RU-lock, мёртвая EN-кнопка скрыта; тела вычищены от рунглиша.
- **Все RU-блоки** статей переведены/вычищены до натива: 23 бренда, ~30 огрызков → полный перевод, ~50 двуязычных статей/сравнений, substrate/use-case/encyclopedia.

### QA — рескан = 0 (репозиторно)
| Проверка | Результат |
|---|---|
| Рунглиш `[латиница]'[кириллица]` | **0** |
| Огрызки-дисклеймеры (RU-сводка / EN-версия выше) | **0** |
| «Меда декоративи» | **0** |
| flagship RU-lock | **4** |
| precise dirty-detector (stub ∪ runglish ∪ EN-back-link-in-RU ∪ ruonly-rung) | **0** |
| EN-блоки vs `integration` (byte-identical) | **100 двуязычных файлов — НИ ОДИН не изменён** |

**Коммиты:** `7c72e00` (волна 1, 24 файла) · `8877402` (волна 2) · `333d4f3` (волна 3 — финал тела).

## 5. NARYAD-чеклист
1. Огрызки → полный перевод — ✅  2. Рунглиш убит (рескан=0) — ✅  3. 4 флагмана залочены — ✅
4. RU back-link + стрэй-английские заголовки *внутри RU-блоков* — ✅ (nav-партиалы lib:foot/crumb — см. §6)
5. Меда → русский термин — ✅  6. Структурный i18n тела (тоггл реально меняет контент) — ✅

## 6. Открытый структурный слой — nav-партиалы (требует решения)
`scripts/build-article-nav.js` генерит `lib:crumb` (хлебные крошки) и `lib:foot` («Связанные статьи» / next / CTA) + нормализует back-link. Сейчас на **94 статьях** эти блоки в **английском** (Related articles / Get the set / A curated reading path / breadcrumb «Library»), т.к. в `library-manifest.json` у 97 из 106 статей `lang:"en"`.

Почему НЕ пофикшено в этом проходе (нужен твой выбор):
- **Дефолт-локаль.** Перевести = выставить всем `lang:"ru"` (+ RU-заголовки в манифесте) → nav станет RU **для всех**, включая EN-браузеры. Это тот самый структурный i18n-выбор, что я флагал. Затрагивает не только статьи, но и плитки library.html / SEO (манифест — общий источник).
- **back-link нормализатор небезопасен для dual-block** (строка 105: глобальный regex заменит back-link и в EN-, и в RU-блоке на один язык → перезатрёт мой per-block перевод / сломает EN). Перед ре-раном скрипт надо пропатчить (dual-block-aware).
- **Заголовки карточек/крошек** берутся из `manifest.title` (английские) → нужна локализация заголовков манифеста (RU-h1 уже есть в телах — можно собрать).

**Рекомендация (отдельный мини-пасс):** пропатчить build-article-nav.js (dual-block-safe back-link, RU-метки) + локализовать заголовки в манифесте из RU-h1 + ре-ран + QA. ~1 короткий проход, детерминированный (без субагентов).

## 7. Deliverables
- Ветка `fix/ru-native-translation`, коммиты выше + этот FIXLOG.
- PR (без мёржа). Прод/configurator/эталоны не тронуты.
