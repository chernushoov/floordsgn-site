# floordsgn-dropin-inventory.md — инвентарь ветки feature/studio-product-dropin

> **Происхождение:** реконструировано из проверенного диффа эталон↔дропин + NARYAD 03 (2026-06-09).
> Оригинал в репозитории отсутствовал. Оператор: ревью / заменить оригиналом. Источник правды — код.

## Назначение
Что несёт `feature/studio-product-dropin` относительно чистого эталона (`8f7a6a5`), с разметкой **оставить / выкинуть**, чтобы будущие наряды переносили хирургически и **не пересобирали мёрж-гниль**. Связано: [[STUDIO_ARCHITECTURE.md]] §2, §4.

## Вердикт
**Сами фичи готовы** — гнильём оказался только слой «как показать Studio» (Слои-мёрж). Пересобирать от эталона, черри-пикать точечно. **Не мержить дропин целиком.**

## Уже В ЭТАЛОНЕ (трансплант НЕ нужен — проверено в NARYAD 03)
Эталон `8f7a6a5` уже чистый и со всеми фичами: **persona-контроллер, persona-gate, A/B-сравнение, lead-with-persona** — все на месте, **без Слои-вкладки**.

## Дельта дропина над эталоном
`studio.js` diff = 67 ins / 22 del; `floor-room.html` = +211 строк.

- 🔴 **ВЫКИНУТЬ — Слои/Layers-вкладка (мёрж-гниль, корень коллапса):**
  `STATE.tab`; `LAYER_LABELS`/`LAYER_ORDER`/`buildLayersData`/`showLayersNow`/`setTab`/`buildViewTabs`; синк `if (STATE.tab==='layers') showLayersNow()` в `renderPanel`; вызов `buildViewTabs()` в init. Плюс `floor-room.html` +211 строк = движок 3D-разреза (`showLayers`/`getLayerAnchors`/`isLayerMode`). **Никогда не приносить.**
- 🟢 **ОСТАВИТЬ (единственная полезная дельта) — мобильный фикс 09.06:** guard `matchMedia('(max-width:1024px)')` в init (телефон/планшет не walled). *Применён в NARYAD 03 R3.*
- 🟡 **Смежные твики дропина (НЕ гниль, но вне scope NARYAD-03 — решать отдельно):**
  life-size scale на всех материалах (было только при чипах); цвет/RAL на всех персонах (было исключено для warehouse); секция «Дизайн/Pattern» всегда présente.

## Прочее
- `3d-assets/studio-personas.json` — **идентичен** на эталоне и дропине (9 персон).
- Правильная замена Слои-вкладки = card-link на отдельный `configurator.html` (см. [[STUDIO_ARCHITECTURE.md]] §4).
