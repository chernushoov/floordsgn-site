# 2026-05-23

## A11y Recovery
- MoltBot factory showed `floordsgn_quality_batch` failing with `MODULE_NOT_FOUND` for `a11y.js`.
- Restored `a11y.js` only, without touching the many in-progress HTML edits from other agents.
- Verified `node --check a11y.js`, `npm run a11y`, and `CI=1 npm run a11y`; 8 pages scanned successfully with warnings only.
- Queued recovery task `tsk_1779544697959_cf685925`; it finished `done`, and MoltBot project status showed FloorDSGN healthy again.
- Protected etalon surfaces remain untouched: `index.html` hero-3D, `configurator.html`, and `landing.html`.

## Live Domain Smoke
- User reported `floordsgn.com` is live. Verified `https://floordsgn.com/` and `https://www.floordsgn.com/` return HTTP 200 through Cloudflare Pages (`floordsgn.pages.dev`).
- Checked `/`, `/blog`, `/floors`, `/decision-tool`, `/contact`, `/robots.txt`, `/sitemap.xml`, and `/en/`: all reachable; browser smoke found no failed requests or page errors on sampled pages.
- Sitemap has 35 URLs and all resolve with HTTP 200 after redirects. SEO cleanup needed: sitemap still lists `.html` URLs while Cloudflare Pages redirects most to extensionless paths.
- Language switch works on sampled pages (`/`, `/blog`, `/floors`): RU and EN H1 swap and `html lang` updates after `setLanguage('en')`. Static titles are mixed: several RU views still have English `<title>`, and `/en/` initially ships with `html lang="ru"` before client-side language handling.
